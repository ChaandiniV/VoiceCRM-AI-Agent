# GitHub Push Steps

```bash
cd voicecrm-ai-agent

git init
git add .
git commit -m "Initial VoiceCRM AI Agent project"

git branch -M main
git remote add origin https://github.com/<your-username>/voicecrm-ai-agent.git
git push -u origin main
```

Recommended repository description:

```text
Voice AI + CRM automation portfolio project using FastAPI, Make webhooks, Google Sheets/HubSpot-ready workflow, SQLite, and Streamlit dashboard.
```

Recommended GitHub topics:

```text
voice-ai, fastapi, make-com, webhooks, crm, hubspot, streamlit, ai-agent, solutions-engineering
```
